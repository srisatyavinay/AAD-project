#include "headers.h"

void dfs(ptrgraph g, int start)
{
    
}

void dfs_init(ptrgraph g, int src)
{
    // int newarr[g->num_vertices]; // Create a global variable

    // for(int i = 0; i < g->num_vertices; i++)
    // {
    //     newarr[g->gr[i].id] = 0; // Look at corner cases when deleting a vertex and also check remaining functions
    // }

    // for(int i = 0; i < )
    // dfs(g, src);
}
